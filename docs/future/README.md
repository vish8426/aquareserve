# Future Work Assets

Material kept for the commercial follow-on.

- `aero-theme.css` - a complete Frutiger Aero / Windows XP glass stylesheet (frosted translucent panels and tabs, luna-gradient titlebars, glossy gel controls).
- It was trialled on the single React app, then set aside because the **results dashboard** should keep its green presentation style.
- This CSS is the starting point for the **separate customer product app**: the configurator plus the future live control and monitoring UI.
- Its class names match the current components, so dropping it into a product app that reuses those components would restyle them wholesale.
