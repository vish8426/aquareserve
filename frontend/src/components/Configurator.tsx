// Moved out of the results dashboard as part of the product-app split.
// The customer configurator now lives in the separate product app at product/src/components/Configurator.tsx.
// The results dashboard is results-only, so this file is intentionally a stub (kept because the mounted filesystem does not allow deleting it here).
export {};
